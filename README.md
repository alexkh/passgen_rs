This is a desktop application for generating random passwords.

![App Screenshot](doc/screenshot.png)

This app is written in Rust.

To build type:

```cargo build --release```

This will produce an executable target/release/passgen
